@model IEnumerable<CommunitySpaceShipData.SharePackage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Neighbor.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Shareable.ShareableName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShareEvent.ShareEventID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Neighbor.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Shareable.ShareableName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShareEvent.ShareEventID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SharePackageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SharePackageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SharePackageID })
        </td>
    </tr>
}

</table>
